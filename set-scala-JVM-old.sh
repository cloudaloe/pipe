echo "run this script with source or . preceded to it, in order for it to export into the shell."
echo "that will make scala use the JVM exported to the environment here, rather than the OS default one (the OS default is of course what you get when running 'java')"
export JAVA_HOME=/usr/local/oracleJDK/jdk1.7.0_13

